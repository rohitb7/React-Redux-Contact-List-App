/*
import React from 'react';
import { renderComponent , expect } from '../test_helper';


import { mount, shallow } from 'enzyme';

import sinon from 'sinon';

import CreateContact from '../../src/components/createContact';
import RaisedButton from 'material-ui/RaisedButton';

/*
describe('Test CreateContact' , () => {

	let component; //App

	beforeEach(() => {
  		component = renderComponent(CreateContact);
  	})

	it('has a form' , () => {
		expect(component.to.have.class('create-contact'));
	});

	it('has a button' , () => {
		expect(component.find('RaisedButton')).to.exist;
	});

	it('has a correct class create-contact' , () => {
		expect(component.find('RaisedButton')).to.exist;
	});



	describe('Entering some texts' , () => {

		beforeEach(() => {
  			component = renderComponent(CreateContact);
  		})
	

		it('shows text in name TextField' , () => {
			expect(component.find('.create-name')).to.exist;
		});


	});



})






describe('<CreateContact/>', () => {

	  it('should render 1 <CreateContact /> components', () => {
	    const wrapper = shallow(<CreateContact/>);
	    expect(wrapper.find('CreateContact')).to.have.length(1);
	  });


	  it('simulates click events', () => {
	    //const onButtonClick = sinon.spy();
	    const wrapper = shallow(
	      <CreateContact onButtonClick={onButtonClick} />
	    );
	    wrapper.find('RaisedButton').simulate('click');
	    expect(onButtonClick.calledOnce).to.equal(true);
	  });


});
*/



