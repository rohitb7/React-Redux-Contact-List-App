/*
import React from 'react';
import { renderComponent , expect } from '../test_helper';
import App from '../../src/components/app';

import sinon from 'sinon';


import { mount, shallow } from 'enzyme';
import MuiThemeProvider from 'material-ui/styles/MuiThemeProvider';

import CreateContact from '../../src/components/createContact';
 


		describe('<App />', () => {

		  it('should render 1 <App /> components', () => {
		    const wrapper = shallow(<App />);
		    expect(wrapper.find(App)).to.have.length(1);
		  });



		  it('should render children when passed in', () => {
		    const wrapper = shallow(
		      <App>
		        <div className="unique" />
		      </App>
		    );
		    expect(wrapper.contains(<div className="unique" />)).to.equal(true);
		  });


		  //MuiThemeProvider is a nested component
		   it('should render MuiThemeProvider ', () => {
			    const wrapper = shallow(<MuiThemeProvider/>);
			    expect(wrapper.containsAllMatchingElements([
			      	<MuiThemeProvider/>
			    ])).to.equal(true);
			});


		   it('should render 1 <CreateContact /> components', () => {
		    const wrapper = shallow(<CreateContact/>);
		    expect(wrapper.find('CreateContact')).to.have.length(1);
		  });

		});
	*/



	 






